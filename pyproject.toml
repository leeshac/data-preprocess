[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "datapreprocessing"
version = "0.0.1"
requires-python = ">=3.11"
readme = "README.md"
authors = [{name = "Aleesha Adekoya"}]
dependencies = [ "pandas", "numpy", "scikit-learn" ]